// export default function SidebarButton({ children, parameter, handleClick }) {
// 	return (
// 		<button
// 			onClick={() => handleClick(parameter)}
// 			className={`${
// 				parameter ? 'w-full' : 'w-squares'
// 			} h-squares rounded-xl flex items-center justify-center bg-black/50 border-2 text-white hover:scale-110 transition-transform duration-300`}>
// 			{children}
// 		</button>
// 	)
// }
