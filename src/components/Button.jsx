// import { buttonClasses } from '../../data/buttonClasses'
// export default function Button({ type, children, item, categories, handleClick, handleShowModal }) {
// 	return (
// 		<button
// 			onClick={() => {
// 				item == undefined || handleClick(item?.id)
// 				handleShowModal(true)
// 			}}
// 			className={`${
// 				buttonClasses.find(button => button.name === type)?.classes
// 			} flex items-center justify-center bg-black/50 border-2 text-white hover:scale-110 transition-transform duration-300`}>
// 			{children}
// 		</button>
// 	)
// }
